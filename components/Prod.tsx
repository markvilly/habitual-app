import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Prod = () => {
  return (
    <View>
      <Text>Prod</Text>
    </View>
  );
};

export default Prod;

const styles = StyleSheet.create({});
